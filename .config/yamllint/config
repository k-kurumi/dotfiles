---
extends: default

rules:
  # 1行200文字まで
  line-length:
    max: 200
    level: warning

  # CF風の配列にインデント入れないパターン
  indentation:
    spaces: 2
    indent-sequences: whatever

  # デフォルトはコメント前に2スペースが必要なので無効に上書き
  comments:
    min-spaces-from-content: disable
